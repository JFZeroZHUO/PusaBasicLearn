image: m.daocloud.io/docker.io/library/node:22-alpine

stages:
  - build

variables:
  NEXT_TELEMETRY_DISABLED: "1"

cache:
  key:
    files:
      - package-lock.json
  paths:
    - .npm/

build:
  stage: build
  script:
    - npm ci --include=optional --cache .npm --prefer-offline
    - npm run build
  artifacts:
    name: "build-$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHORT_SHA"
    expire_in: 1 week
    when: on_success
    paths:
      - .next/static/
      - public/legacy/index.html
      - public/legacy/assets/*.js
      - public/legacy/assets/*.css
